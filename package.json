{
  "name": "pastekey",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "bun run typecheck && vite build",
    "preview": "vite preview",
    "verify": "bun run test && bun run build && wrangler deploy --dry-run",
    "deploy": "bun run verify && wrangler d1 migrations apply pastekey --remote && wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply pastekey --local",
    "db:migrate:remote": "wrangler d1 migrations apply pastekey --remote",
    "typecheck": "bun run typecheck:client && bun run typecheck:worker && bun run typecheck:tools",
    "typecheck:client": "tsc -p tsconfig.client.json",
    "typecheck:worker": "tsc -p tsconfig.worker.json",
    "typecheck:tools": "tsc -p tsconfig.tools.json",
    "test": "vitest run",
    "smoke:production": "bun scripts/smoke.ts"
  },
  "dependencies": {
    "@cloudflare/kumo": "2.7.0",
    "@phosphor-icons/react": "2.1.10",
    "@simplewebauthn/server": "13.3.2",
    "hono": "4.12.29",
    "react": "19.2.7",
    "react-dom": "19.2.7"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "1.44.0",
    "@cloudflare/vitest-pool-workers": "0.18.4",
    "@cloudflare/workers-types": "5.20260711.1",
    "@types/react": "19.2.17",
    "@types/react-dom": "19.2.3",
    "@vitejs/plugin-react": "6.0.3",
    "typescript": "7.0.2",
    "vite": "8.1.4",
    "vitest": "4.1.10",
    "wrangler": "4.110.0"
  }
}
